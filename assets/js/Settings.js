// These settings can be overridden by creating a file named 'Settings.js' in root path

var settings = {
  // Base url to Rosette server
  rosetteBaseUrl : "http://localhost:9000/api/v1-snapshot/",

  // Set this url to get events from old event server
  oldEventsServerUrl : null,
  
  // Enable data to show
  showPosters : true,
  showBookings : true,
  showEvents : true,
  showBibelnSe : true,
  
  logoSrc : './assets/img/logotype.jpg',
  
  welcomeContent : '<p>till oss...</p>'
};
